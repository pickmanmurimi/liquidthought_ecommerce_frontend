export type Address = {
    uuid?: string,
    full_name?: string,
    address?: string,
    postal_code?: number,
    city?: string,
    state?: string,
    country?: string,
    default?: boolean,
}